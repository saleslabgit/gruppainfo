# Техническое задание: кабинет психологов

---

# 1. Цель проекта

Разработать отдельное Laravel-приложение — личный кабинет психологов и административную панель для существующего сайта-каталога психологических групп.

Существующий публичный сайт продолжает выполнять функции каталога и содержит:

- форму регистрации психолога;
- формы записи участников в группы.

Новое приложение отвечает за:

- регистрацию и модерацию психологов;
- авторизацию психологов;
- управление группами;
- оплату размещения;
- продление размещения;
- модерацию групп;
- контроль срока размещения;
- получение заявок участников;
- административное управление пользователями, группами, платежами и справочниками.

Автоматическая публикация групп в существующий каталог **не входит в MVP**. Администратор публикует группу вручную и после этого отмечает её активной в кабинете.

---

# 2. Стек

## Backend

- Laravel 12.x.
- PHP 8.2+.
- MySQL.
- Composer.

## Frontend

- Blade.
- Bootstrap 5.
- собственный CSS;
- Vanilla JS только там, где без него неудобно.

Не использовать:

- Vue;
- React;
- Inertia;
- Livewire;
- Tailwind;
- Vite;
- npm;
- Node.js.

Все CSS/JS находятся непосредственно в `public/`. Bootstrap подключается локальным файлом с зафиксированной версией, не через CDN.

Bootstrap используется как техническая база компонентов и сетки. Внешний вид задаётся собственной дизайн-системой.

## Локальная разработка

Docker Compose окружение:

- PHP 8.2;
- MySQL;
- веб-сервер;
- Composer.

Исходный код хранится обычными файлами на компьютере и подключается в контейнер.

Проект должен запускаться одной командой.

## Production

Docker не используется.

На хостинг загружается обычный Laravel-проект.

Production:

- PHP 8.2.32 или совместимый;
- MySQL;
- document root направлен на `public/`;
- cron используется для Laravel Scheduler;
- cron или supervisor используется для обработчика очередей.

## Совместимость окружений

Поскольку разработка ведётся в Docker, а production работает без него, расхождение версий и расширений PHP должно быть исключено на уровне конфигурации:

- в `composer.json` фиксируется `config.platform.php` равным версии production;
- в `composer.json` перечисляются все требуемые расширения PHP в секции `require`;
- перед выкладкой выполняется `composer check-platform-reqs`;
- список требуемых расширений дублируется в инструкции по установке.

---

# 3. Роли

В MVP две роли.

## Администратор

Имеет доступ ко всем данным приложения.

Может:

- просматривать всех психологов;
- создавать психологов вручную;
- редактировать;
- отключать;
- удалять;
- принимать или отклонять анкеты;
- назначать психолога бесплатным/платным;
- просматривать документы психолога;
- просматривать все группы;
- создавать и редактировать группы;
- модерировать группы;
- отправлять группу на доработку;
- отклонять группу;
- одобрять группу;
- отмечать группу опубликованной/активной;
- вручную удалять брошенные черновики;
- просматривать заявки участников;
- просматривать платежи;
- отмечать выполненный возврат платежа;
- управлять справочниками и настройками.

В MVP администратор один, но архитектура не должна препятствовать появлению нескольких администраторов.

Все действия администратора, меняющие статус психолога, группы или платежа, фиксируются с указанием автора действия (см. §4.8).

## Психолог

Может:

- войти в кабинет после одобрения;
- просматривать информацию о себе;
- просматривать свои группы;
- создавать группы;
- редактировать группу на разрешённых этапах;
- удалять черновые/неактивные группы;
- отправлять группу на модерацию;
- оплачивать размещение, если он платный;
- продлевать размещение;
- видеть статус каждой своей группы;
- видеть историю замечаний администратора по своей группе;
- видеть заявки только своих групп;
- отмечать заявку обработанной.

Психолог **не видит раздел истории платежей**. Стоимость и факт оплаты конкретной операции он видит только в рамках самой операции оплаты.

---

# 4. Основные архитектурные решения

## 4.1. `status` — единственный источник правды

Исходные поля:

- `accept`;
- `disabled`;
- `free`;
- `admin`

можно сохранить в схеме, но **строить workflow на `accept=true/false` запрещено**.

У пользователя и у группы существует явный `status`.

Требования:

- `status` — единственный источник правды о стадии жизненного цикла;
- `accept` не используется в бизнес-логике, условиях, выборках и шаблонах;
- если `accept` сохраняется для совместимости с существующим сайтом, он вычисляется из `status` и записывается **только в одном месте** — в model observer или доменном сервисе;
- контроллеры, формы и seed-данные не имеют права писать `accept` напрямую.

`disabled`, `free` и `admin` остаются отдельными независимыми признаками и не входят в `status`.

## 4.2. Статусы как перечисления и машина состояний

Статусы пользователя, группы и платежа реализуются PHP-enum (backed enum, string) с кастами Laravel.

Для каждого enum описывается допустимость переходов, например метод `canTransitionTo(self $target): bool`.

Требования:

- ни один переход статуса не выполняется присваиванием поля напрямую из контроллера;
- переход выполняется через доменный сервис, который проверяет допустимость перехода и выбрасывает исключение при недопустимом;
- в БД используется строковая колонка (`varchar`), а не MySQL `enum`, чтобы не усложнять миграции;
- допустимый набор значений и матрица переходов покрываются автоматическими тестами.

## 4.3. Удаление

Использовать soft delete для основных сущностей.

Физическое удаление пользователя или группы не должно уничтожать:

- платежи;
- историю заявок;
- историю статусов;
- связанные данные.

## 4.4. Бесплатность группы

`gp_users.free` показывает текущий тариф психолога.

При создании группы значение копируется в `gp_groups.free`.

Таким образом, если администратор позже поменяет тариф психолога, старые группы не меняются задним числом.

## 4.5. Оплата

Успешный redirect пользователя со страницы банка **не является подтверждением оплаты**.

Единственный источник подтверждения — валидный webhook банка.

Webhook должен обрабатываться идемпотентно: повторная отправка одного webhook не должна создавать повторную оплату или повторно менять состояние группы.

Оплата в MVP бывает двух типов:

- `placement` — оплата размещения новой группы;
- `extension` — оплата продления размещения существующей группы.

## 4.6. Денежные суммы

Все денежные суммы хранятся **целым числом в минорных единицах** (копейках) в колонках типа `unsigned bigint`.

Использование `float` запрещено. `decimal` не используется, чтобы исключить расхождения округления при сверке с банком.

Валюта хранится отдельным полем, значение по умолчанию — `BYN`.

Форматирование для отображения выполняется единым helper/Blade-компонентом.

## 4.7. Дата и время

- в БД всё хранится в UTC;
- `APP_TIMEZONE=UTC`;
- отображение и ввод дат выполняются в `Europe/Minsk`;
- преобразование выполняется единым helper/Blade-компонентом, а не вручную на каждой странице;
- вся логика сроков размещения работает с UTC.

## 4.8. История статусов и аудит

Единственного поля «комментарий модератора» недостаточно: при повторных доработках комментарий будет перезаписан и психолог потеряет предыдущие замечания.

Создаётся таблица `gp_group_status_history`:

- `id`;
- `group_id`;
- `from_status` — nullable;
- `to_status`;
- `actor_id` — кто выполнил переход, nullable для системных переходов;
- `actor_type` — `user` / `system`;
- `comment` — nullable;
- `created_at`.

Каждый переход статуса группы создаёт запись истории в той же транзакции, что и сам переход.

Психолог видит историю замечаний по своей группе.
Администратор видит полную историю.

Аналогично фиксируются ключевые действия над психологом (принятие, отклонение, отключение, смена тарифа) — минимально через ту же механику или отдельную таблицу истории по решению разработчика, но фиксация автора действия обязательна.

## 4.9. Очереди

Отправка писем и обработка отложенных задач выполняются через очередь Laravel на драйвере `database`.

Причина: синхронная отправка писем внутри scheduled-команды приводит к падению всей команды при таймауте SMTP и к потере части уведомлений.

Требования:

- таблица очереди создаётся миграцией;
- таблица `failed_jobs` создаётся миграцией;
- на production обработчик очереди запускается через supervisor либо через cron с `queue:work --stop-when-empty`;
- признак фактической отправки письма проставляется только внутри job после успешной отправки; успешная постановка job в очередь сама по себе не считается отправкой;
- для писем, которые должны отправляться один раз за период, должна быть защита от повторной постановки одинаковой job до завершения первой попытки (см. §27).

---

# 5. Регистрация психолога

## Сценарий

1. Пользователь заполняет анкету на существующем сайте.
2. Сайт серверным POST-запросом передаёт данные в новое приложение.
3. Создаётся `gp_users`.
4. Пользователь получает статус `pending`.
5. Администратор видит новую анкету.
6. Администратор выбирает: принять или отклонить.
7. При принятии:
   - пользователь получает статус `approved`;
   - на email отправляется одноразовая ссылка установки пароля.
8. Психолог устанавливает пароль.
9. После этого может войти в кабинет.

До принятия войти невозможно.

Администратор должен иметь возможность повторно отправить письмо установки пароля.

## Уникальность email и повторная подача анкеты

Email психолога уникален среди активных (не soft-deleted) записей.

Поведение при поступлении анкеты с уже существующим email определяется статусом существующей записи:

| Статус существующей записи | Поведение |
|---|---|
| `pending` | Данные существующей анкеты обновляются, новые документы добавляются. HTTP 200. |
| `rejected` | Запись возвращается в `pending`, данные обновляются, администратор видит её как новую. HTTP 200. |
| `approved` | Новая анкета не создаётся. HTTP 409 с понятным сообщением. Событие логируется. |
| `disabled=true` | Новая анкета не создаётся. HTTP 409. Событие логируется. |
| soft-deleted | Новая анкета не создаётся автоматически. HTTP 409. Восстановление выполняет администратор вручную. |

На уровне БД уникальность активного email реализуется технической generated column, например `active_email`, которая содержит `email` только при `deleted_at IS NULL`, а для soft-deleted записей — `NULL`. На эту колонку создаётся уникальный индекс.

Это гарантирует, что одновременно может существовать только одна активная запись с конкретным email, но soft-deleted запись не блокирует последующее ручное создание новой записи с тем же email.

Техническая generated column не используется в бизнес-логике приложения.

Использовать уникальный индекс `(email, deleted_at)` **запрещено**: в MySQL несколько строк с `NULL` в составе уникального индекса допустимы, поэтому такой индекс не гарантирует уникальность активного email.

Простой уникальный индекс по `email` также использовать нельзя: при soft delete удалённая запись навсегда заблокирует повторное использование адреса.

## Установка пароля

- используется штатный механизм Laravel (password broker), самописные токены не создаются;
- срок жизни ссылки задаётся конфигурацией, по умолчанию 72 часа;
- токен одноразовый, после использования инвалидируется;
- повторная отправка письма администратором инвалидирует предыдущую ссылку;
- на запрос установки пароля и на форму входа устанавливается rate limit.

---

# 6. Анкета психолога

В `gp_users` хранятся основные данные анкеты:

- фамилия;
- имя;
- отчество;
- телефон;
- email;
- тип образования;
- другое образование;
- модальность / программа;
- учебный центр;
- год окончания;
- количество часов;
- номер и срок действия лицензии;
- опыт ведения групп;
- количество проведённых групп;
- подтверждение достоверности документов;
- подтверждение соответствия образования;
- готовность провести вебинар или эфир;
- `personal_data_consent_at` — дата и время согласия на обработку персональных данных;
- `personal_data_consent_version` — версия текста согласия;
- `status`;
- `accept`;
- `disabled`;
- `free`;
- `admin`;
- `password`;
- timestamps;
- soft delete.

Приложение обрабатывает персональные данные психологов, копии дипломов и лицензий, а также ФИО и телефоны участников групп. Факт и версия согласия на обработку персональных данных фиксируются при приёме анкеты. Публичный сайт обязан передавать эти значения вместе с анкетой.

Поскольку администратор хранится в той же таблице, все поля анкеты должны быть nullable, а для администратора обязательными остаются только email, пароль и признак `admin`.

Модель `User` явно указывает `protected $table = 'gp_users';`.

## Документы

Документы хранятся отдельной сущностью `gp_user_documents`:

- `id`;
- `user_id`;
- `type`;
- `path`;
- `original_name`;
- `mime_type`;
- `size`;
- timestamps.

Типы документов:

- диплом;
- сертификат;
- лицензия / членство;
- свидетельство о государственной регистрации.

Требования к файлам:

- файлы не лежат в публично доступной директории, хранение в `storage/app/private`;
- имя файла на диске генерируется случайным, оригинальное имя хранится в БД отдельным полем;
- проверяется реальный тип содержимого файла, а не расширение;
- допустимые типы: `pdf`, `jpeg`, `png`;
- максимальный размер файла задаётся конфигурацией;
- просмотр и скачивание выполняются через контроллер Laravel после проверки прав доступа (владелец или администратор).

---

# 7. Статусы психолога

Основные:

### `pending`
Анкета ожидает проверки.

### `approved`
Анкета принята.

### `rejected`
Анкета отклонена.

Допустимые переходы:

- `pending → approved`;
- `pending → rejected`;
- `rejected → pending` (повторная подача анкеты, см. §5);
- `approved → rejected` — не используется, для отключения используется `disabled`.

## Признак `disabled`

`disabled` существует отдельно от `status`.

Если `disabled=true`, пользователь не может войти независимо от его статуса.

**Блокировка действует немедленно, включая уже открытые сессии.**

Требования:

- middleware проверяет `disabled` и статус пользователя на каждом запросе в защищённой зоне;
- при обнаружении блокировки текущая сессия завершается, пользователь разлогинивается с понятным сообщением;
- при установке `disabled=true` администратором все активные сессии пользователя инвалидируются немедленно (смена `remember_token` и/или удаление сессий пользователя из хранилища сессий);
- те же правила действуют при soft delete пользователя и при переводе в `rejected`.

Хранилище сессий — `database`, чтобы инвалидация сессий конкретного пользователя была технически возможна. Соответствующая миграция создаётся на этапе 2.

---

# 8. Кабинет психолога

Основная навигация:

- Мои группы;
- Мои данные;
- Выход.

На главной странице сразу показывается список групп и кнопка **«Добавить группу»**.

Для каждой группы показывать:

- название;
- статус;
- формат;
- дату создания;
- дату публикации;
- дату окончания размещения;
- количество новых заявок;
- кнопку просмотра;
- доступные действия.

Если групп нет — понятное empty state с кнопкой добавления.

---

# 9. Создание бесплатной группы

Если `gp_users.free=true`:

1. Психолог нажимает «Добавить группу».
2. Создаётся черновая группа с `free=true`, статус `draft`.
3. Открывается форма группы.
4. Психолог заполняет данные.
5. Нажимает «Отправить на модерацию».
6. Группа получает статус `moderation`.

Платёж не создаётся.

---

# 10. Создание платной группы

Если `gp_users.free=false`:

1. Психолог нажимает «Добавить группу».
2. Создаётся технический черновик `gp_groups` со статусом `awaiting_payment` и `free=false`.
3. Создаётся запись `gp_payments` с типом `placement`.
4. Пользователь перенаправляется в платёжный интерфейс выбранного банка/эквайера.
5. Банк возвращает пользователя обратно в приложение.
6. Приложение ждёт подтверждение webhook.
7. После успешного webhook:
   - платёж получает статус `succeeded`;
   - группа переходит из `awaiting_payment` в `draft`.
8. Пользователю становится доступна форма заполнения группы.
9. После заполнения он отправляет группу на модерацию.
10. Группа получает статус `moderation`.

Если redirect произошёл раньше webhook, показывать:

**«Оплата подтверждается банком»**

и кнопку обновления страницы.

Заполнение группы до подтверждения webhook невозможно.

Если оплата неуспешна, пользователь может повторить оплату. Для новой попытки создаётся новая запись платежа. Предыдущая неуспешная запись сохраняется.

## Отклонение оплаченной группы и возврат средств

Если оплаченная группа отклонена администратором (`moderation → rejected`):

- указание причины отклонения **обязательно**, причина сохраняется и показывается психологу;
- возврат денежных средств выполняется администратором **вручную через личный кабинет эквайринга**, автоматический возврат в MVP не реализуется;
- в карточке отклонённой группы администратору показывается связанный платёж с `transaction_id`, суммой и датой, чтобы возврат можно было выполнить без обращения к БД;
- после выполнения возврата администратор отмечает платёж возвращённым: платёж получает статус `refunded`, заполняются `refunded_at` и комментарий;
- отметка о возврате — учётное действие внутри приложения, никаких запросов в банк оно не выполняет;
- отклонённая группа не даёт права на бесплатное создание новой: чтобы разместить группу заново, психолог создаёт новую группу и оплачивает её как обычно.

## Брошенные черновики

Черновики в статусах `awaiting_payment` и `draft`, по которым психолог не завершил работу, **автоматически не удаляются**.

Администратору предоставляется:

- фильтр списка групп по статусу и дате создания, позволяющий быстро найти брошенные записи;
- возможность удалить такую группу вручную (soft delete);
- запрет на удаление группы, по которой существует успешный платёж без отметки о возврате — в этом случае показывается предупреждение с требованием сначала обработать платёж.

---

# 11. Поля группы

`gp_groups`:

- `id`;
- `public_uuid`;
- `owner_id`;
- `status`;
- `disabled`;
- `accept`;
- `free`;
- название;
- описание;
- расписание;
- формат — ссылка на справочник;
- длительность одной встречи — количество минут, integer;
- количество человек — integer;
- пол — ссылка на справочник;
- стоимость одной встречи — целое число в копейках;
- `moderator_comment` — последний комментарий модератора, полная история в `gp_group_status_history`;
- `rejection_reason` — причина отклонения;
- `external_catalog_id` — внешний ID группы в каталоге, nullable;
- `published_at`;
- `expires_at`;
- `expiry_warning_sent_at` — дата отправки предупреждения об окончании;
- `placement_days` — фактический срок размещения, зафиксированный на момент активации;
- timestamps;
- soft delete.

`public_uuid` используется при интеграции с публичным сайтом и позволяет не передавать наружу внутренний `id`. Индекс — уникальный.

`placement_days` фиксируется из настроек в момент активации, чтобы последующее изменение настройки не искажало историю.

---

# 12. Статусы группы

## `awaiting_payment`
Ожидает оплаты.

Отображение: **Ожидает оплаты**

## `draft`
Оплата подтверждена либо группа бесплатная, но анкета группы ещё не отправлена.

Отображение: **Черновик**

## `moderation`
Отправлена администратору.

Отображение: **На модерации**

## `revision`
Администратор запросил исправления.

Отображение: **На доработке**

Психолог обязательно видит комментарий администратора и всю историю замечаний.

## `rejected`
Группа отклонена.

Отображение: **Отклонена**

Причина отклонения обязательна и показывается психологу.

## `approved`
Администратор одобрил группу, но ещё не опубликовал её в публичном каталоге.

Отображение: **Одобрена, ожидает публикации**

## `active`
Администратор вручную опубликовал группу в каталоге и нажал «Отметить активной».

Отображение: **Активная**

## `expired`
Срок размещения закончился.

Отображение: **Закончена**

## Матрица допустимых переходов

| Из | В | Кто |
|---|---|---|
| `awaiting_payment` | `draft` | система, по успешному webhook |
| `draft` | `moderation` | психолог |
| `moderation` | `approved` | администратор |
| `moderation` | `revision` | администратор, комментарий обязателен |
| `moderation` | `rejected` | администратор, причина обязательна |
| `revision` | `moderation` | психолог |
| `approved` | `active` | администратор |
| `active` | `expired` | система, по расписанию |
| `expired` | `approved` | система, после успешной оплаты продления (см. §16) |

Любой другой переход недопустим и должен приводить к ошибке независимо от того, как он инициирован — через интерфейс, подделанный POST или прямой вызов сервиса.

`disabled=true` используется отдельно для административного отключения группы и не входит в матрицу переходов.

---

# 13. Модерация группы

Администратор открывает группу и видит:

- данные психолога;
- данные группы;
- информацию об оплате, если группа платная — сумма, `transaction_id`, статус, дата;
- текущий статус;
- полную историю статусов и комментариев.

Действия:

### Одобрить

`moderation → approved`

### Отправить на доработку

`moderation → revision`

Комментарий обязателен. Комментарий сохраняется в историю и не затирает предыдущие.

Психолог редактирует группу и повторно отправляет её:

`revision → moderation`

### Отклонить

`moderation → rejected`

Причина отклонения обязательна, минимальная длина проверяется валидацией.

Если группа платная и по ней есть успешный платёж, интерфейс отклонения показывает напоминание о необходимости ручного возврата и данные платежа.

### Отметить активной

Доступно после ручной публикации в существующем каталоге:

`approved → active`

При этом:

- `published_at = текущее время`;
- `placement_days` = значение из настроек;
- `expires_at = published_at + placement_days`;
- `expiry_warning_sent_at` сбрасывается в null.

При необходимости администратор указывает внешний ID опубликованной группы.

---

# 14. Ограничения редактирования группы психологом

Психолог может редактировать группу в состояниях:

- `draft`;
- `revision`.

Просмотр доступен для всех его групп.

В `moderation`, `approved`, `active`, `expired` редактирование блокируется, чтобы данные кабинета не расходились с данными, которые проверяет или уже опубликовал администратор.

Удалять психолог может только группы в статусах `draft` и `rejected`, по которым нет успешного неотозванного платежа.

Удаление является soft delete.

Администратор имеет полный доступ независимо от статуса.

---

# 15. Срок размещения

Срок размещения начинается **не с оплаты и не с одобрения**, а с момента ручной публикации администратором.

При активации:

`expires_at = published_at + placement_days`

Значение по умолчанию — 30 дней, хранится в настройках.

За N дней до `expires_at` (по умолчанию 3):

- психолог получает email;
- в списке групп появляется заметная отметка: **«До окончания размещения 3 дня»**.

Письмо отправляется только один раз для текущего периода размещения. Признак — `expiry_warning_sent_at`.

После наступления `expires_at`:

`active → expired`

Психолог видит статус **Закончена** и кнопку **«Продлить размещение»**.

Поскольку публикация в каталоге выполняется вручную, автоматическое удаление группы с публичного сайта в MVP не выполняется. Администратор видит такие группы в отдельном фильтре и снимает их с публикации вручную.

---

# 16. Продление размещения

Продление доступно для групп в статусах `active` и `expired`.

Повторная модерация при продлении **не выполняется**: содержание группы уже проверено.

## Бесплатный психолог

1. Психолог нажимает «Продлить размещение».
2. Платёж не создаётся.
3. Применяется логика продления (см. ниже).

## Платный психолог

1. Психолог нажимает «Продлить размещение».
2. Создаётся запись `gp_payments` с типом `extension` и ссылкой на группу.
3. Пользователь перенаправляется в банк.
4. Продление применяется только после успешного webhook.
5. При неуспешной оплате продление не применяется, психолог может повторить попытку — создаётся новая запись платежа.

Стоимость продления берётся из настроек. В MVP она может совпадать со стоимостью размещения.

## Логика продления

### Группа в статусе `active`

- `expires_at = expires_at + placement_days`;
- статус не меняется, группа остаётся опубликованной;
- `expiry_warning_sent_at` сбрасывается в null, чтобы предупреждение пришло заново перед новым окончанием;
- действия администратора не требуются.

### Группа в статусе `expired`

- группа переходит `expired → approved`;
- `published_at` и `expires_at` не устанавливаются в момент оплаты;
- администратор видит группу в списке ожидающих публикации с пометкой «Продление», публикует её в каталоге вручную и нажимает «Отметить активной»;
- при активации даты рассчитываются как при первичной публикации.

Причина: снятие группы с публикации при истечении срока выполняется администратором вручную, поэтому возврат в каталог тоже требует ручного действия. Повторная модерация при этом не производится — администратор только публикует.

Продление группы в статусе `expired` доступно в течение срока, заданного настройкой (по умолчанию 30 дней после `expires_at`). После этого кнопка продления скрывается и психолог создаёт новую группу.

---

# 17. Заявки участников

Существующий сайт передаёт в приложение:

- `group_uuid`;
- фамилию;
- имя;
- телефон.

Создаётся `gp_group_applications`:

- `id`;
- `group_id`;
- фамилия;
- имя;
- телефон;
- `phone_normalized` — телефон в нормализованном виде для поиска и выявления дублей;
- `processed_at`;
- timestamps.

## Правила приёма

Заявка принимается **только для группы в статусе `active`**.

На публичном сайте форма заявки отображается только у опубликованных групп, поэтому в норме нарушение невозможно, однако приложение обязано проверять статус самостоятельно:

- группа не найдена по `public_uuid` → HTTP 404;
- группа найдена, но не в статусе `active` либо `disabled=true` → HTTP 422 с понятным кодом ошибки;
- запрос логируется в обоих случаях.

Телефон нормализуется при сохранении: убираются пробелы, скобки и дефисы, номер приводится к международному формату.

## Доступ

Психолог:

- видит только заявки принадлежащих ему групп;
- не имеет возможности открыть заявки другого психолога;
- может отметить заявку обработанной;
- может вернуть её обратно в необработанные.

В интерфейсе группы показывается количество новых, обработанных и всех заявок.

## Хранение персональных данных участников

Заявки содержат персональные данные третьих лиц.

- срок хранения заявок задаётся настройкой, по умолчанию 12 месяцев с даты создания;
- scheduled-команда удаляет заявки старше этого срока;
- удаление — окончательное, включая soft-deleted записи;
- команда логирует количество удалённых записей.

---

# 18. Интеграция с существующим сайтом

Нужно два входящих сценария.

## Общие требования

- все интеграционные маршруты располагаются под префиксом версии: `/api/v1/...`;
- маршруты исключены из web-middleware и не используют CSRF;
- используется только HTTPS;
- ограничение по IP-адресам публичного сайта, если это технически возможно;
- rate limit на каждый маршрут;
- единый формат ответа об ошибке: HTTP-код, машиночитаемый `code`, человекочитаемое `message`, при ошибках валидации — `errors`;
- все ошибочные и отклонённые запросы логируются с телом запроса и причиной отказа.

## Аутентификация запросов

Простого статического секрета в заголовке недостаточно — он уязвим к повторной отправке перехваченного запроса.

Используется подпись запроса:

- заголовок `X-Signature` — HMAC-SHA256 от тела запроса с общим секретом;
- заголовок `X-Timestamp` — время формирования запроса;
- запросы с расхождением времени более 5 минут отклоняются;
- сравнение подписей выполняется функцией защищённого сравнения (`hash_equals`);
- секрет хранится только в `.env` на серверах обеих систем;
- секрет не размещается в HTML или JavaScript публичного сайта.

## Идемпотентность

Публичный сайт передаёт заголовок `X-Request-Id` — уникальный идентификатор попытки.

Приложение хранит обработанные идентификаторы (таблица или кэш) и при повторном получении того же `X-Request-Id` возвращает результат первой обработки, не создавая дубликат.

Это защищает от дублей при повторной отправке запроса сайтом по таймауту.

## Регистрация психолога

Существующий сайт отправляет серверный POST со следующим содержимым:

- поля анкеты;
- факт и версия согласия на обработку персональных данных;
- файлы документов (multipart).

Поведение при повторном email описано в §5.

## Заявка в группу

Существующий сайт отправляет серверный POST:

- `group_uuid`;
- фамилия;
- имя;
- телефон.

Поведение описано в §17.

## Документация

По результатам этапа интеграции готовится отдельный документ для разработчика публичного сайта: адреса, формат запросов, формирование подписи, коды ответов, примеры запросов.

---

# 19. Платежи

## Выбор банка / эквайринга

Конкретный банк или платёжный провайдер на момент подготовки ТЗ **не определён**.

До начала этапа 13 требуется дополнительная информация:

- выбранный банк/эквайер и официальная документация интеграции;
- тестовое окружение и тестовые реквизиты/ключи;
- способ создания платежа и правила redirect/return/cancel URL;
- способ проверки подлинности webhook;
- формат webhook и матрица внешних статусов платежа;
- правила и формат `transaction_id` или эквивалентного идентификатора;
- требования к IP allowlist, сертификатам или дополнительным механизмам безопасности, если они есть;
- фактические правила ручного/автоматического возврата, необходимые для корректного отображения информации администратору.

До получения этих данных допускается реализовывать только провайдер-независимую доменную модель платежей и интерфейс адаптера. Придумывать или заранее реализовывать банк-специфичные запросы, подписи, поля и webhook-протокол запрещено.

Таблица `gp_payments`:

- `id`;
- `owner_id`;
- `group_id`;
- `type` — `placement` / `extension`;
- `transaction_id` — уникальный индекс;
- `amount` — целое число в копейках;
- `currency` — по умолчанию `BYN`;
- `status`;
- `paid_at` — дата успешной оплаты;
- `refunded_at` — дата отметки о выполненном возврате;
- `refund_comment`;
- `bank_response` — ответ банка при создании платежа, json, nullable;
- timestamps;
- soft delete.

Статусы платежа:

- `created`;
- `pending`;
- `succeeded`;
- `failed`;
- `cancelled`;
- `refunded`.

Допустимые переходы:

- `created → pending → succeeded | failed | cancelled`;
- `succeeded → refunded`;
- обратные переходы из `succeeded` в `failed` или `cancelled` **запрещены**.

## Обработка webhook

Требования:

- маршрут webhook исключён из CSRF-проверки и из middleware авторизации — иначе банк может отклонить или повторять отправку;
- подлинность webhook проверяется средствами, предусмотренными выбранной банковской интеграцией;
- вся обработка выполняется в транзакции с блокировкой строки платежа (`lockForUpdate`);
- повторная обработка того же события не меняет состояние и не создаёт дубликатов;
- переход платежа выполняется только если он допустим по матрице;
- изменение статуса группы выполняется в той же транзакции, что и изменение платежа;
- сырое тело каждого входящего webhook сохраняется в отдельную таблицу `gp_payment_webhooks` (`id`, `payment_id` nullable, `transaction_id` nullable, `payload`, `signature_valid`, `processed`, `result`, `created_at`) — включая запросы, отклонённые по подписи или не найденные по `transaction_id`;
- webhook всегда возвращает банку корректный HTTP-код по правилам выбранного провайдера, даже если событие уже было обработано.

## Административный доступ

Администратор видит:

- дату;
- психолога;
- группу;
- тип платежа;
- transaction ID;
- сумму;
- статус/результат;
- дату и комментарий возврата.

Психолог отдельного раздела платежей не имеет.

## Изоляция банковской логики

Вся работа с банком выносится в отдельный сервис/адаптер. В контроллерах не должно быть ни формирования запросов к банку, ни разбора его ответов.

Интерфейс адаптера должен позволять подменить его тестовой реализацией в автотестах.

---

# 20. Справочники

Значения, которые могут меняться, не должны быть захардкожены в Blade/PHP.

Создать `gp_dictionaries` и `gp_dictionary_items`.

Минимально через них хранить:

- тип образования;
- формат группы;
- пол;
- другие повторно используемые варианты анкеты/группы.

Элемент справочника:

- `id`;
- `dictionary_id`;
- стабильный `code`;
- отображаемое название;
- сортировка;
- активность.

Удалённый из использования элемент деактивируется, а не удаляется физически, если он уже используется в данных.

Администратор имеет простой CRUD справочников.

---

# 21. Настройки

Изменяемые бизнес-параметры не хранить непосредственно в PHP.

Минимально вынести:

- стоимость размещения платной группы;
- стоимость продления;
- срок размещения — по умолчанию 30 дней;
- срок предупреждения — по умолчанию 3 дня;
- срок, в течение которого доступно продление после окончания — по умолчанию 30 дней;
- срок хранения заявок участников — по умолчанию 12 месяцев;
- срок жизни ссылки установки пароля.

Используется таблица `gp_settings`. Доступ к значениям — через сервис с кэшированием и типизацией, а не прямыми запросами из шаблонов.

Критически важные секреты остаются только в `.env`:

- база;
- SMTP;
- ключи банка;
- integration secret.

---

# 22. Административная часть

## Пользователи

Таблица психологов:

- ФИО;
- email;
- телефон;
- статус;
- бесплатный/платный;
- включён/отключён;
- дата регистрации.

Функции:

- поиск;
- фильтр по статусу;
- фильтр бесплатный/платный;
- просмотр;
- создание;
- редактирование;
- удаление;
- принять;
- отклонить;
- отключить;
- повторно отправить установку пароля.

## Группы

Таблица:

- ID;
- название;
- психолог;
- статус;
- бесплатная/платная;
- дата создания;
- дата публикации;
- дата окончания.

Поиск: ID, название, психолог.

Фильтры: статус, бесплатная/платная, наличие успешного платежа.

Отдельные быстрые фильтры:

- ожидают публикации (`approved`);
- завершённые, требующие снятия с публикации (`expired`);
- брошенные черновики (`awaiting_payment` и `draft` старше N дней).

Сортировка: дата создания, дата публикации, дата окончания.

## Платежи

Отдельный список с поиском и фильтрацией по статусу, типу, психологу, периоду.

Действие «Отметить возврат выполненным».

## Заявки

Администратор может просматривать все заявки.

## Справочники и настройки

CRUD.

---

# 23. Интерфейс психолога

## Мои группы

Основной раздел.

Карточка/строка группы должна сразу отвечать на вопросы:

- что это за группа;
- какой сейчас статус;
- нужно ли от психолога действие;
- когда закончится размещение;
- сколько новых заявок.

Статусы визуально различаются.

Если необходимы исправления, комментарий администратора должен быть заметен без дополнительных поисков.

## Мои данные

Показываются данные анкеты психолога.

В MVP самостоятельное изменение анкеты психологом не требуется.

## Группа

Показывается:

- информация о группе;
- статус;
- комментарий администратора и история замечаний;
- даты размещения;
- список заявок;
- доступные действия, включая продление.

---

# 24. Дизайн-система

Создание UI-kit является отдельным этапом разработки **до массового создания страниц**.

Bootstrap используется только как foundation.

Создать собственные CSS tokens:

- основные цвета;
- фон;
- текст;
- muted text;
- border;
- success;
- warning;
- danger;
- размеры шрифта;
- line-height;
- border radius;
- spacing;
- shadows.

На Stage 3 создать переиспользуемую baseline-систему Blade-компонентов/partials:

- primary button;
- secondary button;
- danger button;
- text input;
- textarea;
- select;
- checkbox;
- radio;
- label;
- validation error;
- alert;
- карточка;
- status badge;
- таблица;
- pagination;
- modal;
- dropdown;
- empty state;
- navbar/header;
- sidebar/navigation;
- page header;
- confirmation dialog;
- отображение даты и времени;
- отображение денежной суммы.

Полный каталог и визуальные правила остаются зафиксированы в `DESIGN_SYSTEM.md`, но специализированные компоненты добавляются инкрементально, когда впервые требуются продуктовому этапу. Перед использованием на продуктовой странице отсутствующий компонент сначала реализуется в общей дизайн-системе. Страничная визуальная замена общего компонента запрещена. Unresolved-значения требуют отдельного решения пользователя/дизайнера.

Создать отдельную страницу UI-kit с реализованными baseline-компонентами и состояниями: normal, hover, focus, disabled, error, success.

Все дальнейшие страницы собираются из этих компонентов. Копировать индивидуальные стили между страницами запрещено.

Собственный CSS подключается после Bootstrap.

---

# 25. Responsive

Кабинет должен полноценно работать на desktop, tablet, smartphone.

На мобильном:

- навигация адаптируется;
- формы занимают доступную ширину;
- кнопки не выходят за экран;
- длинные таблицы либо прокручиваются горизонтально, либо отображаются в мобильном представлении;
- основные действия доступны без hover;
- модальные окна помещаются в viewport.

---

# 26. Email

Необходимые письма MVP:

### Психолог принят
Ссылка установки пароля.

### До окончания размещения N дней
Название группы и дата окончания.

Дополнительно администратор должен иметь возможность повторно отправить письмо установки пароля.

Отправка писем использует стандартную почтовую инфраструктуру Laravel, настройки SMTP из `.env` и очередь (§4.9).

---

# 27. Scheduler

Laravel Scheduler используется для:

- проверки приближающегося окончания групп и постановки писем в очередь;
- перевода просроченных групп в `expired`;
- удаления заявок участников с истёкшим сроком хранения.

На production настраивается cron запуска Laravel Scheduler и обработчик очередей.

## Требования к идемпотентности

- обработка каждой группы выполняется в транзакции с `lockForUpdate`;
- для предупреждения об окончании scheduler ставит отдельную job в очередь, но не отмечает письмо отправленным;
- для одной группы и одного текущего периода размещения одновременно может существовать только одна ожидающая/выполняемая job предупреждения; используется уникальный lock/idempotency key, привязанный минимум к `group_id` и текущему `expires_at`;
- повторный запуск scheduler, пока первая job ещё ожидает или выполняется, не создаёт дубликат;
- `expiry_warning_sent_at` проставляется job только после фактически успешной отправки письма;
- если job была поставлена в очередь, но фактическая отправка завершилась ошибкой, `expiry_warning_sent_at` остаётся `null`, а повторная попытка выполняется механизмом очереди;
- после успешной отправки повторные запуски scheduler не создают новое письмо для того же периода размещения;
- если группа активирована так, что до окончания уже меньше порога, письмо отправляется один раз при первом применимом запуске;
- команды пишут в лог итог работы: сколько записей обработано.

---

# 28. Технические требования

## Индексы

Обязательные индексы:

- `gp_users`: уникальный индекс по технической generated column активного email согласно §5; индексы по `status`, `disabled`, `free`; уникальный `(email, deleted_at)` не использовать;
- `gp_groups`: уникальный `public_uuid`, индексы по `owner_id`, `status`, `expires_at`, составной `(status, expires_at)` для scheduler, составной `(owner_id, status)`;
- `gp_payments`: уникальный `transaction_id`, индексы по `owner_id`, `group_id`, `status`, `type`;
- `gp_group_applications`: индексы по `group_id`, `processed_at`, составной `(group_id, processed_at)`, индекс по `created_at` для команды очистки;
- `gp_group_status_history`: индекс по `group_id`;
- `gp_dictionary_items`: составной `(dictionary_id, code)` уникальный.

## Запросы

- все списки используют eager loading, N+1 запросы недопустимы;
- счётчики заявок в списке групп реализуются через `withCount`, а не запросом на каждую строку;
- все списки административной части имеют пагинацию;
- в тестах списков проверяется отсутствие N+1 (подсчёт количества запросов).

## Транзакции

Любая операция, меняющая несколько сущностей одновременно (переход статуса + запись истории, обработка webhook + изменение группы, продление + изменение платежа), выполняется в одной транзакции.

## Качество кода

- Laravel Pint с зафиксированным конфигом;
- статический анализ (Larastan) на согласованном уровне;
- server-side валидация через Form Request для всех входящих запросов;
- авторизация через policies, проверка в контроллерах обязательна.

## Тесты

Помимо тестов, указанных в приёмке этапов, обязательны:

- тесты матрицы переходов статусов: каждый недопустимый переход приводит к ошибке;
- IDOR-тесты для групп, заявок, документов и платежей;
- тест идемпотентности webhook;
- тесты scheduler с подменой времени (`Carbon::setTestNow`);
- тест уникальности email при наличии soft-deleted записи;
- тест немедленного разлогинивания заблокированного пользователя.

Тесты выполняются на MySQL в Docker-окружении, а не на SQLite, чтобы поведение индексов и транзакций совпадало с production.

---

# 29. Безопасность

Обязательно:

- Laravel CSRF для внутренних форм;
- серверная validation всех запросов;
- password hashing стандартными средствами Laravel;
- authorization через middleware / policies;
- психолог не может получить чужую группу через изменение URL;
- психолог не может получить чужую заявку;
- документы анкеты не доступны напрямую через `public/`;
- webhook банка проверяется средствами, предусмотренными банковской интеграцией;
- маршруты webhook и интеграции исключены из CSRF и из auth-middleware;
- integration secret и ключи банка хранятся только в `.env`;
- подписи и секреты сравниваются через `hash_equals`;
- заблокированный пользователь теряет доступ немедленно, включая открытые сессии (§7);
- rate limit на login, установку пароля и интеграционные маршруты;
- production работает с `APP_DEBUG=false`;
- все секреты только в `.env`.

Особое внимание тестам IDOR: изменение `group_id`, `user_id`, `payment_id` или `application_id` в URL/POST не должно давать доступ к чужим данным.

---

# 30. Что не входит в MVP

Не реализовывать без отдельной задачи:

- автоматическую публикацию в существующий каталог;
- автоматическое удаление публикации из существующего каталога;
- автоматическую сверку статусов платежей опросом банка;
- автоматический возврат средств через API банка;
- автоматическую очистку брошенных черновиков;
- Vue/React;
- SPA;
- realtime;
- WebSockets;
- frontend build;
- раздел истории платежей для психолога;
- ручную регистрацию оплаты администратором;
- сложные роли и permissions;
- чат;
- push-уведомления;
- мобильное приложение;
- аналитику;
- CRM;
- редактирование опубликованной группы психологом;
- повторную модерацию при продлении.

---

# 31. Этапы разработки

Каждый этап является отдельной законченной задачей Codex.

Нельзя переходить к следующему этапу, пока текущий нельзя запустить и проверить.

## Принцип очерёдности

Сначала должен быть полностью собран и проверен внутренний продукт: авторизация, CRUD психологов, кабинет психолога, CRUD групп, модерация, справочники, настройки, lifecycle размещения и внутренняя работа с заявками.

До конца этапа 10 внешние входящие формы публичного сайта, SMTP/email и банковская интеграция не требуются для ручной продуктовой проверки. Для локальной проверки используются development/test seed/factory-данные и действия внутри административной панели и кабинета.

Этап 11 подключает входящие формы существующего сайта к уже проверенному внутреннему продукту. Этап 12 добавляет email/onboarding и уведомления. Этап 13 добавляет оплату размещения, этап 14 — платное продление. Это позволяет подключать внешние зависимости по одной и не смешивать их с базовой CRUD-логикой.

До этапа 13 все сценарии создания групп в интерфейсе проходят по бесплатному пути независимо от текущего `gp_users.free`. Статус `awaiting_payment`, типы платежей и таблицы платежей существуют в схеме с этапа 2, но платный пользовательский flow остаётся недоступным до банковского этапа.

---

## Этап 1. Базовый проект, окружение и документация

Сделать:

- Laravel, PHP 8.2, MySQL, веб-сервер, Composer;
- Docker Compose;
- `.env.example`;
- подключение к БД;
- базовый layout Blade;
- Bootstrap локальным файлом;
- собственный `app.css`, `app.js`;
- тестовую страницу;
- Laravel Pint с конфигом;
- Larastan с зафиксированным уровнем;
- `APP_TIMEZONE=UTC`, helper отображения дат в `Europe/Minsk`;
- helper отображения денежных сумм;
- фиксацию `config.platform.php` и списка расширений в `composer.json`;
- завести базовую документацию проекта:
  - `README.md` — назначение проекта, требования, запуск одной командой, основные команды и проверки;
  - `docs/architecture.md` — структура приложения, границы слоёв, расположение доменных сервисов, enum-статусов и ключевые архитектурные соглашения;
  - `docs/development.md` — локальное окружение, `.env`, Docker, migrations, seed, тесты, Pint, Larastan и правила разработки;
  - `docs/project-status.md` — текущий реализованный этап, что уже работает, известные ограничения и открытые технические вопросы;
- документация должна описывать фактическое состояние проекта, а не будущую реализацию; в последующих этапах связанные документы обновляются вместе с кодом, а `docs/project-status.md` — в каждом этапе.

Не использовать Node/npm/Vite.

### Приёмка

1. Проект запускается одной командой.
2. Открывается в браузере.
3. Laravel подключается к MySQL.
4. CSS/Bootstrap работают.
5. npm отсутствует и для запуска не требуется.
6. `php artisan test` работает.
7. Pint и Larastan запускаются и проходят.
8. `composer check-platform-reqs` проходит.
9. `README.md` и базовые документы в `docs/` существуют, соответствуют фактическому состоянию этапа 1 и содержат проверенные команды запуска/проверок.

---

## Этап 2. База данных и доменная модель

Создать миграции и модели:

- `gp_users`;
- `gp_user_documents`;
- `gp_groups`;
- `gp_group_status_history`;
- `gp_payments`;
- `gp_payment_webhooks`;
- `gp_group_applications`;
- `gp_dictionaries`;
- `gp_dictionary_items`;
- `gp_settings`;
- таблицы сессий, очереди и `failed_jobs`.

Сделать:

- enum-классы статусов пользователя, группы и платежа;
- матрицу допустимых переходов внутри enum;
- доменные сервисы переходов статусов с записью истории;
- связи моделей;
- индексы по §28;
- generated column активного email и уникальный индекс по ней согласно §5; `(email, deleted_at)` не использовать;
- хранение денежных сумм в копейках с кастами;
- сервис доступа к настройкам с кэшированием;
- начальные справочники, настройки и тестового администратора через seed.

### Приёмка

1. База создаётся с нуля через migrations.
2. Seed создаёт администратора, справочники и настройки.
3. Работают связи User → Groups → Applications → Payments → StatusHistory.
4. Повторный запуск миграций/seed не ломает приложение.
5. Тест подтверждает, что недопустимый переход статуса выбрасывает исключение.
6. Тест подтверждает, что одновременно нельзя создать две активные записи с одинаковым email, а после soft delete можно создать новую запись с тем же email.
7. Тест подтверждает, что переход статуса создаёт запись истории.
8. Денежные суммы корректно сохраняются и отображаются.

---

## Этап 3. UI-kit и дизайн-система

До реализации основных страниц создать фундамент и baseline дизайн-системы. Специализированные компоненты полного каталога добавлять в следующих этапах перед их первым продуктовым использованием.

Сделать:

- CSS tokens;
- baseline Blade UI components по §24;
- страницу UI-kit;
- desktop/mobile состояния.

### Приёмка

В браузере существует одна development/testing-страница, на которой можно проверить реализованные кнопки, формы, ошибки, статусы, карточки, таблицы, модальные окна, навигацию, empty state, отображение дат и сумм, desktop и mobile.

После утверждения baseline UI-kit последующие страницы используют его компоненты. Если экрану нужен ещё не реализованный компонент из `DESIGN_SYSTEM.md`, сначала расширяется общая дизайн-система и только затем собирается экран; unresolved-решения не заполняются самостоятельно.

---

## Этап 4. Авторизация и контроль доступа

Сделать внутренний authentication/access foundation без внешних форм и email:

- штатную session-auth Laravel на guard `web`;
- единую страницу login и POST login с rate limit;
- logout только POST с CSRF;
- регенерацию session ID после успешного входа и корректное завершение сессии при logout;
- защищённые route groups для администратора и психолога;
- middleware проверки на каждом защищённом запросе: пользователь существует, не soft-deleted, `status=approved`, `disabled=false`;
- разделение доступа по признаку `admin`: психолог не открывает `/admin`, обычный психологический кабинет не используется как обход admin-прав;
- немедленный отзыв доступа уже авторизованного пользователя при `disabled=true`, `rejected` или soft delete;
- переиспользуемый механизм инвалидации database sessions конкретного пользователя для последующих админских действий;
- минимальные защищённые landing pages `/admin` и `/cabinet` только для проверки auth/role boundaries, без Stage 5/6 CRUD;
- idempotent development/testing seed одобренного психолога с известными локальными credentials; production seed таких credentials не создаёт;
- UI страницы входа и защищённых acceptance surfaces на общей дизайн-системе, desktop/mobile.

Не делать на этом этапе регистрацию, внешний `/api/v1`, password setup/reset по email, SMTP и отправку писем.

### Приёмка

1. Development admin входит штатными локальными credentials и попадает в защищённую admin-зону.
2. Development psychologist входит штатными локальными credentials и попадает в `/cabinet`.
3. Неверный email/пароль не создаёт authenticated session и возвращает безопасную ошибку без раскрытия существования аккаунта.
4. `pending`, `rejected`, `disabled` и soft-deleted психолог не получает доступ к защищённой зоне.
5. Психолог получает 403/эквивалентный отказ на `/admin`; гость перенаправляется на login.
6. После успешного login session ID регенерируется; logout завершает session и защищённая страница снова недоступна.
7. Если уже вошедшему психологу выставить `disabled=true` или перевести его в недопустимое состояние, следующий защищённый запрос немедленно завершает его доступ.
8. Общий session invalidator удаляет все database sessions указанного пользователя и пригоден для вызова Stage 5 при disable/reject/delete.
9. Login rate limit покрыт тестом.
10. Автоматические тесты проверяют admin/psychologist/guest boundaries и access revocation.
11. Страница login и минимальные защищённые surfaces работают на desktop и mobile и используют только общий UI namespace.

---

## Этап 5. Административный CRUD психологов

Сфокусировать следующий этап на управлении психологами внутри кабинета, без публичной анкеты и без email-onboarding.

Сделать:

- полноценный admin layout;
- список психологов с поиском, фильтрами и пагинацией;
- просмотр всех полей психолога;
- создание психолога вручную;
- редактирование;
- soft delete;
- approve / reject через доменный переход статуса;
- переключение free/paid;
- disabled/enabled;
- фиксацию автора значимых действий;
- подтверждение опасных действий;
- приватный CRUD/просмотр документов психолога из админки: upload, download/view, delete согласно §6;
- обязательное использование session invalidator из Stage 4 при disable/reject/delete.

Письмо установки пароля, повторная отправка письма и внешний приём анкеты остаются Stage 11–12. Администратор не получает временную функцию ручного назначения продуктового пароля. Для проверки входа используется development psychologist из Stage 4.

### Приёмка

1. Администратор создаёт, просматривает, редактирует и soft-delete психолога только через кабинет.
2. Работают поиск, фильтры и пагинация; список не создаёт N+1.
3. Approve/reject выполняются через доменные переходы, фиксируется автор действия.
4. Смена тарифа сохраняется и не меняет существующие группы задним числом.
5. Disable/reject/delete немедленно инвалидирует активные сессии психолога.
6. Администратор загружает и открывает приватный документ; прямого публичного URL к файлу нет.
7. Психолог не открывает ни один admin route и не получает чужие документы через подмену ID.
8. Новые вручную созданные психологи не получают email и не требуют временного password flow на этом этапе.

---

## Этап 6. Кабинет психолога

Сделать:

- общий layout кабинета;
- страницу «Мои группы»;
- страницу «Мои данные»;
- вывод данных текущего психолога и его документов в разрешённом объёме;
- empty states;
- mobile.

### Приёмка

Психолог входит и:

- видит только себя и свои данные;
- не имеет доступа к admin;
- видит пустой список групп до Stage 7;
- не может получить данные/документы другого психолога подменой ID;
- интерфейс работает на мобильном.

---

## Этап 7. CRUD групп и модерация

Полный внутренний workflow без банка. Для проверки все группы проходят бесплатный путь создания независимо от текущего тарифа пользователя.

`draft → moderation → revision/rejected/approved → active`

Сделать:

- создание группы психологом;
- форму и server-side validation группы;
- просмотр;
- редактирование только в `draft` и `revision`;
- soft delete допустимых групп;
- список групп психолога;
- admin CRUD/просмотр групп;
- админский список с поиском, фильтрами, сортировкой, пагинацией и быстрыми фильтрами по §22;
- модерацию: одобрить, на доработку, отклонить;
- обязательный комментарий при доработке и причину при отклонении;
- историю статусов и отображение психологу/администратору;
- ручную активацию после публикации с расчётом `published_at`, `expires_at`, `placement_days`;
- поле `external_catalog_id`;
- ручное удаление брошенных черновиков администратором;
- policies и IDOR-защиту.

На этом этапе не создавать `gp_payments` из UI и не использовать `awaiting_payment` как достижимый пользовательский статус.

### Приёмка

1. Психолог создаёт группу и отправляет на модерацию.
2. Админ отправляет на доработку, комментарий обязателен.
3. Психолог видит комментарий, исправляет, отправляет повторно.
4. Админ отклоняет — причина обязательна, психолог её видит.
5. Админ одобряет и после ручной публикации активирует группу; даты рассчитываются корректно.
6. История содержит все переходы и авторов.
7. Психолог не редактирует группу в `moderation`, `approved`, `active`.
8. Второй психолог не может открыть/редактировать/удалить чужую группу.
9. Недопустимый переход через подделанный POST отклоняется доменным сервисом.
10. Администратор может вручную удалить брошенный черновик.
11. Платёжный flow отсутствует даже для пользователя с `free=false`.

---

## Этап 8. Справочники, настройки и завершение административного CRUD

Довести внутреннее административное управление до состояния, когда продукт не требует прямых изменений MySQL для повседневной проверки.

Сделать:

- CRUD справочников;
- CRUD настроек через существующий typed SettingService;
- пустой/информационный список платежей как подготовленную admin surface без банковского поведения;
- доработать pagination, validation, responsive и empty states всех реализованных admin-страниц;
- подтверждения опасных действий;
- использовать только общие UI-компоненты.

### Приёмка

1. Администратор управляет психологами, группами, справочниками и настройками без прямого обращения к MySQL.
2. Значения справочников не требуют изменения исходного кода.
3. Изменение срока размещения влияет только на новые активации.
4. Деактивированный элемент справочника исчезает из новых форм, но корректно отображается в существующих данных.
5. Пустая платежная surface не создаёт и не имитирует банковские операции.
6. Все административные страницы проходят desktop/mobile проверку.

---

## Этап 9. Lifecycle размещения и бесплатное продление

Реализовать временной lifecycle групп без email и банка.

Сделать:

- scheduled-команду перевода `active → expired` после `expires_at`;
- отображение оставшегося срока/даты окончания в кабинете и админке;
- фильтр завершённых групп, требующих ручного снятия с публикации;
- кнопку и доменную логику бесплатного продления для `active` и `expired`;
- для `active`: сдвиг `expires_at` на `placement_days`, сброс `expiry_warning_sent_at`, статус не меняется;
- для `expired`: переход `expired → approved` с пометкой «Продление», последующая ручная публикация и активация администратором;
- ограничение срока доступности продления из настроек;
- scheduler-тесты с подменой времени и блокировкой конкурентных переходов.

Email warning jobs не создавать — они относятся к Stage 12.

### Приёмка

1. После `expires_at` активная группа становится `expired` ровно один раз.
2. До срока статус не меняется.
3. В кабинете и админке видны дата/остаток срока и статус «Закончена».
4. Бесплатное продление active-группы сдвигает `expires_at`, не меняя статус.
5. Бесплатное продление expired-группы переводит её в `approved` без повторной модерации.
6. После ручной повторной публикации администратор активирует группу и даты рассчитываются заново.
7. После допустимого окна продления кнопка недоступна, прямой POST отклоняется.
8. Продление сбрасывает `expiry_warning_sent_at`, но письмо/queue job не создаётся.

---

## Этап 10. Внутренняя работа с заявками участников

Реализовать пользовательские сценарии заявок без входящего API публичного сайта.

Данные для автоматических тестов создаются factories. Для ручной local/testing проверки допускаются idempotent development fixtures/seed-данные; production UI не получает искусственную функцию создания заявки.

Сделать:

- список заявок конкретной группы у владельца;
- счётчики новых/обработанных/всех в списках групп;
- просмотр заявки;
- processed / unprocessed;
- admin-доступ ко всем заявкам;
- policies и IDOR-защиту;
- scheduled-команду окончательного удаления заявок старше срока хранения;
- нормализованное отображение/поиск телефона на уже сохранённых данных.

Внешний `/api/v1` приёма заявки, HMAC и `X-Request-Id` не входят в этот этап.

### Приёмка

1. На подготовленных fixture-данных психолог видит заявки только своих групп.
2. Второй психолог не видит заявку и не получает её подменой `application_id`.
3. Владелец переключает processed/unprocessed, состояние сохраняется.
4. Счётчики новых/обработанных/всех корректны и не создают N+1.
5. Администратор видит все заявки.
6. Команда очистки удаляет только записи старше настроенного срока.
7. Все сценарии можно проверить через кабинет после загрузки development fixtures, без публичного сайта.

---

## Этап 11. Входящие интеграции с существующим сайтом

Подключить два внешних входящих сценария к уже проверенному внутреннему продукту.

Сделать:

- versioned `/api/v1` endpoints приёма анкеты психолога и заявки участника;
- HMAC-SHA256 подпись, `X-Timestamp`, допустимое окно времени и `hash_equals`;
- rate limit;
- `X-Request-Id` и идемпотентность;
- единый формат ошибок и безопасное логирование;
- обработку повторной анкеты по §5;
- multipart документы анкеты с приватным хранением и существующими правами доступа;
- заявку по `public_uuid` только для `active` и не disabled группы;
- нормализацию телефона при приёме;
- интеграционную документацию для разработчика публичного сайта.

Email после approve не отправлять на этом этапе: onboarding и SMTP относятся к Stage 12.

### Приёмка

1. Подписанная анкета создаёт `pending` и появляется в существующей admin-панели.
2. Неверная/просроченная подпись отклоняется; секрет не раскрывается.
3. Повтор `X-Request-Id` возвращает результат первой обработки без дубля.
4. Повторная анкета соблюдает матрицу §5 для pending/rejected/approved/disabled/deleted.
5. Документы сохраняются приватно и доступны только через существующие authorization rules.
6. Подписанная заявка на active-группу появляется у её владельца в уже реализованном интерфейсе Stage 10.
7. Неактивная/disabled группа получает 422, неизвестный `public_uuid` — 404.
8. Повтор заявки не создаёт дубль.
9. Подготовлена отдельная актуальная документация API для публичного сайта.

---

## Этап 12. Email, установка пароля и уведомления

Подключить почту только после проверки внутреннего CRUD и входящих данных.

Сделать:

- штатный Laravel password broker для первичной установки пароля approved-психолога;
- одноразовую ссылку с настроенным сроком жизни;
- отправку setup email через database queue после approve, когда у пользователя требуется установка пароля;
- повторную отправку setup email администратором с инвалидированием предыдущего токена;
- форму установки пароля и rate limit;
- интеграцию onboarding с уже существующим Stage 4 login;
- scheduled warning за N дней до `expires_at`;
- отдельную queued job письма окончания;
- защиту от повторной постановки/отправки для одного периода размещения;
- `expiry_warning_sent_at` только после фактически успешной отправки;
- SMTP/queue development verification без изменения банковских сценариев.

### Приёмка

1. Approved-психолог без установленного пароля получает setup email и устанавливает пароль один раз.
2. Ссылка истекает и повторно не используется.
3. Resend инвалидирует предыдущую ссылку.
4. После установки пароля психолог входит через Stage 4 login.
5. До warning-порога письмо отсутствует; ровно на пороге ставится одна job.
6. Повторный scheduler не создаёт duplicate job для того же периода.
7. Успешная отправка выставляет `expiry_warning_sent_at`; ошибка отправки не выставляет.
8. После бесплатного продления новый период может получить новое предупреждение.
9. SMTP/queue failure не ломает lifecycle expiration.

---

## Этап 13. Платежи — банковская интеграция размещения

Добавить платный сценарий создания новой группы поверх уже проверенного бесплатного flow.

**До начала этапа требуется дополнительная информация по выбранному банку/эквайеру согласно §19. Без неё этап считается заблокированным и провайдер-специфичная реализация не начинается.**

Сделать:

- payment service/adapter, изолирующий банковскую логику;
- тестовую реализацию адаптера для автотестов;
- конкретный адаптер только по официальной документации провайдера;
- для `free=false`: создание группы в `awaiting_payment` и платежа `placement`;
- redirect/return/cancel flow;
- return page «Оплата подтверждается банком»;
- webhook вне CSRF/auth с provider-specific verification, транзакцией, `lockForUpdate` и идемпотентностью;
- сохранение всех входящих webhook в `gp_payment_webhooks`;
- `awaiting_payment → draft` только после успешного webhook;
- повтор оплаты новой записью платежа;
- данные платежа в модерации;
- ручной refund accounting: `succeeded → refunded`, `refunded_at`, comment;
- запрет удаления группы с успешным неотозванным платежом;
- полноценный admin список платежей с фильтрами.

### Приёмка

1. Платный психолог создаёт `awaiting_payment` + payment; бесплатный — продолжает существующий free flow.
2. До успешного webhook форма платной группы недоступна.
3. Success/return URL сам по себе состояние не меняет.
4. Невалидный webhook ничего не разблокирует и сохраняется для аудита.
5. Failed payment оставляет группу заблокированной; successful переводит payment в `succeeded`, группу в `draft`.
6. Повтор webhook идемпотентен; `succeeded` не откатывается в failed/cancelled.
7. После оплаты работает уже проверенный Stage 7 moderation flow.
8. При отклонении оплаченной группы админ видит transaction data и может отметить ручной refund.
9. Группа с необработанным successful payment не удаляется.
10. Психолог не имеет отдельного payment-history раздела.

---

## Этап 14. Платное продление размещения

Расширить уже проверенный Stage 9 бесплатный extension flow оплатой для `free=false`.

Сделать:

- payment `type=extension`;
- redirect/return/webhook через существующий банковский adapter;
- применение продления только после `succeeded`;
- идемпотентное применение эффекта продления;
- для active — сдвиг `expires_at` и сброс warning state;
- для expired — `expired → approved` и существующий ручной republish flow;
- сохранение бесплатного продления для `free=true`.

### Приёмка

1. Бесплатное продление продолжает работать без платежа.
2. Платное active-продление меняет дату только после успешного webhook.
3. Failed/cancelled payment даты не меняет.
4. Повтор webhook не продлевает группу дважды.
5. Платное expired-продление после success переводит группу в `approved` ровно один раз.
6. После republish активация использует существующую Stage 9 логику.
7. По истечении допустимого окна продление недоступно независимо от способа оплаты.

---

## Этап 15. Финальная приёмка и Production

Проверить весь проект с чистой базой и все внутренние/внешние сценарии вместе.

Подготовить:

- production `.env.example`;
- инструкцию установки, включая список расширений PHP;
- migrations;
- cron для scheduler;
- запуск обработчика очередей;
- SMTP;
- filesystem permissions;
- production caching;
- `APP_DEBUG=false`;
- страницы ошибок 403/404/500;
- backup-предпосылки;
- финальную документацию интеграции для разработчика публичного сайта;
- production verification банковского adapter по предоставленным данным провайдера.

### Сквозная приёмка MVP

MVP считается готовым, если последовательно выполняется сценарий:

1. Психолог отправляет анкету с сайта подписанным запросом.
2. Администратор получает её.
3. Принимает психолога.
4. Психолог получает письмо и устанавливает пароль.
5. Входит.
6. Видит свои данные.
7. Бесплатный психолог создаёт группу без оплаты.
8. Платный психолог создаёт группу только после успешной оплаты.
9. Администратор модерирует группу, возвращает на доработку, затем одобряет.
10. История замечаний сохраняется полностью.
11. После ручной публикации администратор активирует группу.
12. Срок становится 30 дней.
13. За 3 дня приходит email, повторно не приходит.
14. После окончания группа становится «Закончена».
15. Психолог продлевает размещение, платный — через оплату.
16. Заявка с публичного сайта появляется в нужной группе.
17. Заявка на неактивную группу отклоняется.
18. Только владелец видит заявку, подмена ID не даёт доступа.
19. Владелец отмечает заявку обработанной.
20. Администратор видит все платежи, при отклонении оплаченной группы отмечает возврат.
21. Психолог раздела платежей не видит.
22. Заблокированный психолог теряет доступ немедленно.
23. Всё работает на desktop и mobile.
24. Все справочники и настройки находятся в базе.
25. Ни один frontend build tool для работы приложения не требуется.

---

# 32. Требования к работе Codex

Каждый этап выполнять отдельной задачей.

Для каждого этапа Codex должен:

1. сначала изучить существующий код;
2. не переделывать уже работающие части без необходимости;
3. реализовать только текущий этап;
4. создавать migrations вместо ручных изменений БД;
5. переиспользовать UI-kit;
6. добавлять server-side validation через Form Request;
7. добавлять authorization через policies;
8. выполнять переходы статусов только через доменные сервисы, не присваивая поля напрямую;
9. оборачивать многосущностные операции в транзакции;
10. добавлять автоматические тесты для бизнес-критичной логики, включая IDOR и недопустимые переходы;
11. запускать тесты, Pint и Larastan перед завершением;
12. не добавлять Node/npm/Vite;
13. не добавлять новые framework/packages без реальной необходимости;
14. обновлять связанную документацию при изменении архитектуры, setup, интерфейсов или поведения; `docs/project-status.md` обновлять в каждом этапе;
15. документация должна описывать только фактически реализованное состояние и не выдавать планируемое за готовое;
16. в конце задачи сообщать:
    - что сделано;
    - какие файлы изменены;
    - какие migrations добавлены;
    - какие тесты запущены;
    - как вручную проверить этап.

Основной принцип проекта:

**простая Laravel-архитектура без лишней абстракции, но с чётким разделением авторизации, платежей, интеграций, бизнес-статусов и интерфейса.**
